---
// Content for the section title and subtitle
const title = `SampleStack's Workflow`;
const subTitle = `Built for Eurorack and hardware samplers. SampleStack knows the exact requirements for each module and handles the tedious format conversion so you can focus on making music.`;

// Features list for displaying the key highlights

const features = [
  {
    title: "Multi-module support",
    description:
      "Auto-detects your library structure for Morphagene, Squid Salmple, Multigrain, Digitakt, Octatrack, Blackbox, and more. Each module has different requirements. SampleStack knows them all.",
    class:
      "content-center border-b border-dashed border-slate-400 py-5 sm:col-span-2 sm:p-5",
  },
  {
    title: "Instant validation",
    description: `See at a glance which samples are <span class="font-bold">ready</span>, which <span class="font-bold">need conversion</span>, and which have <span class="font-bold">issues</span>. Color-coded badges make it obvious what needs attention.`,
    class: "content-center py-5 sm:row-start-2 sm:p-5 sm:py-10",
  },
  {
    title: "Built-in audio editing",
    description:
      "Preview samples with a zoomable waveform. Add splice markers, detect transients, apply fades, split and crop, all non-destructively. Save when you're ready, or discard and start over.",
    class:
      "content-center border-t border-dashed border-slate-400 py-5 sm:row-span-2 sm:row-start-2 sm:border-l sm:border-t-0 sm:p-5 sm:text-right",
  },
  {
    title: "And more...",
    class: "content-center border-t border-dashed border-slate-400 py-5 sm:p-5",
    moreFeatures: [
      "Batch convert sample rate, bit depth, and channels to match any module's requirements",
      "Organize with tags, ratings, and searchable metadata",
      "Drag samples directly from your master library to module collections",
      "Native macOS app. Fast, private, works offline",
    ],
  },
];
---

<div class="mx-auto max-w-sm px-4 py-10 sm:max-w-xl sm:px-6 md:max-w-4xl md:py-14 lg:max-w-5xl lg:px-8 lg:py-20 xl:max-w-6xl 2xl:max-w-[81rem]">
  {/* Section container for the title and subtitle */}
  <div class="mx-auto mb-10 max-w-2xl text-center lg:mb-14">
    <h2
      class="text-balance text-2xl font-semibold text-slate-800 md:text-3xl md:leading-tight"
    >
      <Fragment set:html={title} />
    </h2>
    <p class="mt-1 text-pretty text-slate-600">
      <Fragment set:html={subTitle} />
    </p>
  </div>

  <div
    class="mx-auto grid px-4 pb-10 sm:grid-cols-2 sm:grid-rows-3 sm:px-6 lg:px-8 lg:pb-14"
  >
    {
      features.map((feature) => (
        <div class={`${feature.class}`}>
          <h2 class="text-2xl font-bold text-slate-600">{feature.title}</h2>

          <p class="mt-2 text-base/6 text-slate-600">
            <Fragment set:html={feature.description || ""} />
          </p>

          {feature.moreFeatures && (
            <ul class="ml-4 mt-4 w-4/5 list-outside list-disc space-y-2 pl-5 text-base/6 text-slate-600 marker:text-purple-500">
              {feature.moreFeatures.map((item) => (
                <li>{item}</li>
              ))}
            </ul>
          )}
        </div>
      ))
    }
  </div>
</div>
